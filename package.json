{
  "name": "video-processing-service",
  "version": "1.0.0",
  "description": "Video processing service for n8n workflows",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "postinstall": "echo 'Installing ffmpeg...' && apt-get update && apt-get install -y ffmpeg || apk add ffmpeg || echo 'FFmpeg installation may have failed - will try runtime detection'"
  },
  "dependencies": {
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1",
    "fluent-ffmpeg": "^2.1.2",
    "ffmpeg-static": "^5.2.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "keywords": ["video", "ffmpeg", "n8n", "processing"],
  "author": "Your Name",
  "license": "MIT",
  "engines": {
    "node": ">=16.0.0"
  }
}
